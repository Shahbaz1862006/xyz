'use client'
import { motion } from 'motion/react'
import { GlassCard } from '@/components/ui/GlassCard'
import { SectionHeadline } from '@/components/ui/SectionHeadline'
const steps = [{ label: 'Choose an amount', value: '1', sub: 'Start with what you want to send', color: '#EF0027' }, { label: 'Add a destination', value: '2', sub: 'Use the person or merchant address', color: 'var(--primary)' }, { label: 'Tap send', value: '3', sub: 'Coinductor handles the rest', color: 'var(--usdt-green)' }]
export function Card1Problem() { return <section className="py-14 md:py-24 px-4 relative"><div className="max-w-6xl mx-auto relative"><GlassCard className="p-5 sm:p-8 md:p-16 text-center overflow-hidden" scrollLinked><div className="relative z-10"><SectionHeadline title="Sending USDT should feel simple." subtitle="You should not have to learn how crypto fees work before sending money. Coinductor keeps that complexity out of your way." align="center" /><div className="grid grid-cols-1 md:grid-cols-3 gap-4 mt-12">{steps.map((step, i) => <motion.div key={step.label} className="glass-card p-6 rounded-xl text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.12 }}><p className="text-xs font-poppins mb-2" style={{ color: 'var(--on-surface-2)', opacity: 0.7 }}>{step.label}</p><p className="font-grifter font-bold text-3xl" style={{ color: step.color }}>{step.value}</p><p className="text-xs font-poppins mt-1" style={{ color: 'var(--on-surface-2)', opacity: 0.6 }}>{step.sub}</p></motion.div>)}</div></div></GlassCard></div></section> }
